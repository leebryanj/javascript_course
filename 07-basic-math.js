// Use the + operator to add 2 numbers 
var sum = 10 + 10;
console.log(sum);

// Use the - operator to subtract 2 numbers
var difference = 45 - 33;
console.log(difference);

// Use the * symbol for multiplication
var product = 8 * 10;
console.log(product);

// Use the / symbol for division
var quotient = 66 / 33;
console.log(quotient);