// Decimals are sometimes referred to as floating point numbers, or floats.

var ourDecimal = 5.7;
var myDecimal = 0.009;

// Multiplying decimals
var product = 2.0 * 2.5;
console.log(product);

// Dividing decimals
var quotient = 4.4 / 2.0;
console.log(quotient);